// Визуальный режим главного экрана (фон + glass UI).
//
// Откат: установи `false` — вернётся прежний стек (HomeScreenBackdrop,
// прежние плотности карточек / CTA / навбара без усиленного glass).
//
// Согласно piligrim_design_spec.md — палитра только через PiligrimColors.
const bool kHomeLayeredBlurGlass = false;
